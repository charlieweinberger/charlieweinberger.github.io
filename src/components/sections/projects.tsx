export default function Projects() {
  return (
    <div id="projects" className="flex flex-col justify-center h-screen bg-blue-400">
      Projects
      {/* Align these in a grid, with each project being a modal. The limited view shows (from top to bottom) an image, the name, the technologies used, and a one-sentence description. */}
    </div>
  );
}
