export default function About() {
  return (
    <div>

      {/* Include name, location, school, and links (github, linkedin, resume). Maybe also an image, or one/two sentences about who I am as a programmer? Make sure this sections looks good! */}

    </div>
  );
}